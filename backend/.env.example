# SQLite (padrão - sem docker)
DATABASE_URL="file:./data/app.db"

# PostgreSQL (modo postgres - rode: docker compose up -d)
# DATABASE_URL="postgresql://kmestre:kmestre@localhost:5432/kmestre"
# POSTGRES_USER=kmestre
# POSTGRES_PASSWORD=kmestre
# POSTGRES_DB=kmestre

PORT=8080